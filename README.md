Set rust to 1.47 to run this project. You can use a directory override to ensure
cargo always builds with the correct rust version. Run the following from the
project directory. 


```
rust override set 1.47
```
